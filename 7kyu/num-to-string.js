//We need a function that can transform a number (integer) into a string. What ways of achieving this do you know?

//my solution
function numberToString(num) {
    return num.toString() // toString methods changes values to strings
}